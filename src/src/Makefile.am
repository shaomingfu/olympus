bin_PROGRAMS=olympus

GRAPHDIR = ../../lib/graph/src

olympus_CPPFLAGS = -I$(GRAPHDIR)
olympus_LDFLAGS = -L$(GRAPHDIR)
olympus_LDADD = -lgraph

olympus_SOURCES = item.h item.cc \
				  group.h group.cc \
				  main.cc
